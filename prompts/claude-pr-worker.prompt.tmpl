PR: __PR_NUMBER__
Title: __TITLE__
URL: __URL__
Stage: __STAGE__
Head SHA: __HEAD_SHA__
Context JSON: __CONTEXT_JSON__
State File: __STATE_FILE__

Last solved comments: __LAST_SOLVED_COMMENTS__
Last solved subcomments: __LAST_SOLVED_SUBCOMMENTS__
Hint: __HINT__

Continuously resolve GitHub PR #__PR_NUMBER__ by driving the Codex review cycle.

Persistent state lives in __STATE_FILE__. Re-read that file at the start of every iteration and update it before exiting. Use file state and git history as memory across Ralph iterations.

Iteration procedure:
1. Read __STATE_FILE__. If it does not exist, initialize it with:
   {"pr_num":"__PR_NUMBER__","repo":null,"last_seen_head_sha":null,"last_review_id":null,"pending_review_thread_ids":[],"failing_checks":[],"last_action":null,"completion":null}
2. Infer the repo from git remote:
   REPO=$(git remote get-url origin | sed 's|.*github\.com[/:]||' | sed 's|\.git$||')
   Then check out the PR branch:
   gh pr checkout __PR_NUMBER__ --repo "$REPO"
3. Read current PR metadata for the current head SHA, review decision, merge state, and status checks. Treat reviews as current only when the review commit_id matches the current head SHA.
4. Fetch reviews for the PR, identify the latest review on the current head, fetch that review's comments, and query unresolved review threads with GraphQL so thread IDs are available for resolution.
5. Inspect CI for the current head. If required checks are failing, inspect the failing logs with gh, fix the issue locally, run the relevant tests, commit, and push.
6. For actionable unresolved review feedback on the current head, make the necessary code changes locally, run the relevant tests, commit, and push. If a comment should not be implemented, reply with concrete reasoning instead of ignoring it.
7. After pushing fixes, reply on each addressed review comment with a short explanation of what changed. Resolve only the threads you actually addressed, using resolveReviewThread.
8. If there is no Codex review on the current head, or you pushed new commits after the last Codex review, post:
   @codex please review this PR.
   Do not post another request if the current head already has an outstanding Codex review and no new commits were added.
9. Success criteria:
   - No more PR review opinions from Codex for the current head
   - required checks are green for the current head
   - PR merge state is mergeable
   If and only if all three are true, write {"completion":"APPROVED"} to __STATE_FILE__, output exactly:
   RESULT_STAGE=finished
   then exit.
10. If success criteria are not met, save updated state and exit normally. Ralph will feed this same prompt back on the next iteration.

Rules:
- Do not sleep or wait inside the iteration.
- Do not lie with the completion state.
- Exit after one pass; Ralph is responsible for the next iteration.
- This worker resets and cleans the git worktree before each run.
- If you change code, you must git add, git commit, and git push __PUSH_REMOTE__ HEAD:__PUSH_REF__ before finishing.
- Unpushed local changes will be lost on the next run.
- Any machine-written business comment must start with: [pr-loop-bot]

GitHub command hints:
- Infer repo from git remote:
  REPO=$(git remote get-url origin | sed 's|.*github\.com[/:]||' | sed 's|\.git$||')
- Check out the PR branch locally:
  gh pr checkout __PR_NUMBER__ --repo "$REPO"
- Read PR metadata:
  gh pr view __PR_NUMBER__ --repo "$REPO" --json headRefOid,reviewDecision,mergeStateStatus,statusCheckRollup
- Read the current head SHA:
  HEAD_SHA=$(gh api repos/$REPO/pulls/__PR_NUMBER__ --jq '.head.sha')
- Get the latest review on the current head only:
  CURRENT_HEAD_REVIEW=$(gh api repos/$REPO/pulls/__PR_NUMBER__/reviews --jq 'map(select(.commit_id == "'"$HEAD_SHA"'")) | sort_by(.submitted_at) | reverse | .[0]')
- Trigger Codex review:
  gh pr comment __PR_NUMBER__ --repo "$REPO" --body "@codex please review this PR."
- Get review comments for a specific review:
  REVIEW_ID=$(echo "$CURRENT_HEAD_REVIEW" | jq -r '.id')
  gh api repos/$REPO/pulls/__PR_NUMBER__/reviews/$REVIEW_ID/comments
- Reply to a review comment:
  gh api repos/$REPO/pulls/__PR_NUMBER__/comments/$COMMENT_ID/replies --field body="<response>"
- Query unresolved review threads with GraphQL:
  gh api graphql -f owner="${REPO%/*}" -f name="${REPO#*/}" -F pr="__PR_NUMBER__" -f query='
    query($owner: String!, $name: String!, $pr: Int!) {
      repository(owner: $owner, name: $name) {
        pullRequest(number: $pr) {
          reviewThreads(first: 100) {
            nodes {
              id
              isResolved
              comments(first: 20) {
                nodes {
                  databaseId
                  body
                }
              }
            }
          }
        }
      }
    }'
- Resolve a review thread:
  gh api graphql -f threadId="$THREAD_ID" -f query='
    mutation($threadId: ID!) {
      resolveReviewThread(input: {threadId: $threadId}) {
        thread {
          id
          isResolved
        }
      }
    }'
- Inspect CI for the PR head:
  gh pr checks __PR_NUMBER__ --repo "$REPO"
  gh run list --repo "$REPO" --commit "$HEAD_SHA" --json databaseId,name,conclusion,event
  gh run view "$RUN_ID" --repo "$REPO" --log-failed

Allowed stage transitions:
- plan -> impl
- impl -> review
- review -> finished
- otherwise keep the current stage

Final line must be exactly one of:
RESULT_STAGE=plan
RESULT_STAGE=impl
RESULT_STAGE=review
RESULT_STAGE=finished
